name: OneDrive

services:
  onedrive:
    image: driveone/onedrive:${VERSION}-debian
    volumes:
      - onedrive_conf:/onedrive/conf
      - ~/OneDrive:/onedrive/data
    restart: unless-stopped

volumes:
  onedrive_conf:
    external: true
